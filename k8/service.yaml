apiVersion: v1
kind: Service
metadata:
  name: springboot-hello-service
  namespace: surjan-namespace
spec:
  type: NodePort
  selector:
    app: springboot-hello
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
